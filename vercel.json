{
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm install && npm --prefix api install",
  "github": {
    "enabled": true,
    "autoAlias": true,
    "silent": false,
    "deploymentEnabled": {
      "main": true
    }
  }
}
